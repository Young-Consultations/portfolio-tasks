name: Route approved task to Codex

on:
  issues:
    types: [labeled, edited]

permissions:
  contents: read
  issues: write
  actions: read

concurrency:
  group: route-approved-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  invalidate-edited-approval:
    if: github.event.action == 'edited'
    runs-on: ubuntu-latest
    steps:
      - name: Require renewed approval after issue edits
        env:
          GH_TOKEN: ${{ secrets.SLUGGER_GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          set -euo pipefail; set +x
          approved="$(gh api \
            "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels" \
            --jq 'any(.[]; .name == "status:approved")')"
          if [[ "$approved" == true ]]; then
            gh api --method DELETE \
              "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels/status%3Aapproved" \
              --silent
          fi

  prepare:
    if: github.event.action != 'edited'
    runs-on: ubuntu-latest
    outputs:
      route: ${{ steps.gate.outputs.route }}
      issue_number: ${{ steps.gate.outputs.issue_number }}
      task_contract_json: ${{ steps.contract.outputs.task_contract_json }}
      delivery_id: ${{ steps.dispatch.outputs.delivery_id }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Apply approval and idempotency gate
        id: gate
        env:
          GH_TOKEN: ${{ secrets.SLUGGER_GITHUB_TOKEN }}
        run: python -m portfolio_tasks.cli route-check "$GITHUB_EVENT_PATH" >> "$GITHUB_OUTPUT"

      - name: Persist durable dispatch marker
        if: steps.gate.outputs.route == 'true'
        id: dispatch
        env:
          GH_TOKEN: ${{ secrets.SLUGGER_GITHUB_TOKEN }}
        run: |
          set -euo pipefail; set +x
          python -m portfolio_tasks.cli prepare-dispatch "$GITHUB_EVENT_PATH" >> "$GITHUB_OUTPUT"
          delivery_id=$(awk -F= '$1 == "delivery_id" {print $2; exit}' "$GITHUB_OUTPUT")
          marker_path=$(awk -F= '$1 == "marker_path" {print $2; exit}' "$GITHUB_OUTPUT")
          gh api --method POST \
            "repos/$GITHUB_REPOSITORY/issues/${{ steps.gate.outputs.issue_number }}/labels" \
            -f 'labels[]=status:dispatching' >/dev/null
          marker=$(cat "$marker_path")
          comments=$(gh api "repos/$GITHUB_REPOSITORY/issues/${{ steps.gate.outputs.issue_number }}/comments?per_page=100")
          if ! jq -e --arg delivery "$delivery_id" \
            'any(.[]; (.body // "") | contains($delivery))' <<< "$comments" >/dev/null; then
            gh api --method POST \
              "repos/$GITHUB_REPOSITORY/issues/${{ steps.gate.outputs.issue_number }}/comments" \
              -f body="$marker" >/dev/null
          fi
          echo "Dispatch marker persisted for delivery $delivery_id." >> "$GITHUB_STEP_SUMMARY"

      - name: Check out organization control-plane files
        if: steps.gate.outputs.route == 'true'
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: Young-Consultations/.github
          ref: ai-sdlc-v2.1.0
          token: ${{ secrets.SLUGGER_GITHUB_TOKEN }}
          path: shared-platform
          persist-credentials: false

      - name: Install pinned shared contracts
        if: steps.gate.outputs.route == 'true'
        run: |
          set -euo pipefail; set +x
          python -m pip install --disable-pip-version-check --no-cache-dir \
            "$GITHUB_WORKSPACE/shared-platform"

      - name: Build and validate canonical task contract
        if: steps.gate.outputs.route == 'true'
        id: contract
        env:
          ISSUE_JSON: ${{ toJSON(github.event.issue) }}
        run: |
          set -euo pipefail; set +x
          printf '%s' "$ISSUE_JSON" > "$RUNNER_TEMP/source-issue.json"
          python -m ai_sdlc_contracts build-task-contract \
            --source-repository "$GITHUB_REPOSITORY" \
            --issue-json "$RUNNER_TEMP/source-issue.json" \
            --execution-mode implement \
            --output "$RUNNER_TEMP/task-contract.json"
          python -m ai_sdlc_contracts validate-task "$RUNNER_TEMP/task-contract.json"
          jq --arg delivery_id "${{ steps.dispatch.outputs.delivery_id }}" \
             --arg requested_branch "${{ steps.dispatch.outputs.requested_branch }}" \
             '. + {delivery_id:$delivery_id, requested_branch:$requested_branch}' \
             "$RUNNER_TEMP/task-contract.json" > "$RUNNER_TEMP/task-contract.enriched.json"
          mv "$RUNNER_TEMP/task-contract.enriched.json" "$RUNNER_TEMP/task-contract.json"
          delimiter="contract_$(openssl rand -hex 16)"
          {
            printf 'task_contract_json<<%s\n' "$delimiter"
            cat "$RUNNER_TEMP/task-contract.json"
            printf '\n%s\n' "$delimiter"
          } >> "$GITHUB_OUTPUT"

  route:
    needs: prepare
    if: needs.prepare.outputs.route == 'true'
    uses: Young-Consultations/.github/.github/workflows/codex-router.yml@ai-sdlc-v2.1.0
    with:
      task_payload: ${{ needs.prepare.outputs.task_contract_json }}
      execution_mode: implement
    secrets:
      CODEX_ROUTER_TOKEN: ${{ secrets.SLUGGER_GITHUB_TOKEN }}

  mark-queued:
    needs: [prepare, route]
    if: always() && needs.prepare.outputs.route == 'true' && needs.route.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Replace approval status after router acceptance
        env:
          GH_TOKEN: ${{ secrets.SLUGGER_GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ needs.prepare.outputs.issue_number }}
        run: |
          set -euo pipefail; set +x
          # Persist the terminal marker first. If approval removal then fails, a
          # redelivery sees status:queued and the live-issue gate will not route
          # the task a second time.
          gh api --method POST \
            "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels" \
            -f 'labels[]=status:queued' >/dev/null
          gh api --method DELETE \
            "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels/status%3Aapproved" --silent || true
          gh api --method DELETE \
            "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels/status%3Adispatching" --silent || true
          echo "Queued canonical delivery ${{ needs.prepare.outputs.delivery_id }}." >> "$GITHUB_STEP_SUMMARY"
