# Portfolio source consumer for AI-SDLC 2.4.4

**Status:** draft consumer repin; merge only after the published 2.4.4
attestation and deployed gates have been verified.

This release selects the immutable `ai-sdlc-v2.4.4` organization router in
place of `ai-sdlc-v2.4.3`. The closed v2 task payload, human approval,
source lifecycle, and result projection remain the same. The portfolio
target's separately pinned receiver remains on its 2.3.2 compatibility unit.

## Merge gate

Keep the source consumer on `ai-sdlc-v2.4.3` until all of these are verified:

1. `codex-adapter-v2.4.4` points to reviewed consulting-playbook commit
   `70ea4342abf7115f6848ea32bb958bbf6be696c1` with registered report
   SHA256 `63052d48e113f9420bd3d9f5e74a070b48d67a07d5ca46a0841c842ebcc6c06e`.
2. `ai-sdlc-v2.4.4` points to reviewed control-plane commit
   `adb57508762168b3410f52e8a7b0151078c6e9b9`; the publication
   attestation is merged and passes publishable validation. The release owner
   accepted the lightweight-tag and review-submission variances recorded in
   the control-plane 2.4.4 release record; neither tag is moved.
3. Deployed `AI-SDLC Runtime Preflight` passes from control-plane `main`
   with `candidate_mode: false` and the immutable REAL preflight passes.
   Retain links to both successful workflow runs as merge evidence.
4. This source consumer PR is reviewed and passes CI on its final head.

After merge, an authorized human may review the exact revision of fresh
source issue #151 and apply `status:approved` to initiate the controlled
REAL implement run. The issue remains queued until then. Preserve the
task, delivery, and correlation identities, target run, receiver result,
source projection, validation, and managed draft PR as acceptance evidence.
Terminal issues #148 and #150 cannot be reused. No preflight success or
source repin by itself authorizes execution.

The immutable published 2.4.3 release remains the rollback baseline.
