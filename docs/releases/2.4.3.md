# Portfolio source consumer for AI-SDLC 2.4.3

**Status:** draft consumer repin; blocked until the 2.4.3 control-plane
publication attestation merges and deployed Runtime Preflight passes.

This release changes the source consumer's reusable-router pin from published
`ai-sdlc-v2.4.2` to `ai-sdlc-v2.4.3`. It does not change the closed v2 task
payload, source approval semantics, lifecycle ownership, or result projection
contract.

## Merge gate

Keep production on `ai-sdlc-v2.4.2` until all of these conditions are true:

1. immutable `codex-adapter-v2.4.3` resolves to reviewed target commit
   `1a5da85a4e29b83ba72e1bf4354d7770035cd367` and its registered report digest
   is `25be867dfc891222cc6fb49b3d9c0c708fee95834176ee464f87ae191f3ba44b`;
2. immutable `ai-sdlc-v2.4.3` resolves to reviewed control-plane commit
   `9b240014d9fd5b5987cd8bfcde742d9b6079f501`;
3. the separate control-plane publication attestation records that identity and
   passes `validate_release.py --require-publishable`;
4. deployed `AI-SDLC Runtime Preflight` runs with `candidate_mode: false` and
   passes remote tag, activation, and credential-metadata checks; and
5. this consumer PR passes review and final-head CI.

After merge, run SIM and live verify with a fresh governed task identity. If
those gates pass, use a separate fresh harmless REAL implement issue and retain
the complete router, target, receiver, source-projection, draft-PR, and
redelivery evidence. Do not reuse terminal issue #148.

Published 2.4.2 remains the rollback/source-consumer baseline and its immutable
tag must not be moved or reinterpreted.
