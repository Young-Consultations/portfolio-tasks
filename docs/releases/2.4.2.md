# Portfolio source consumer for AI-SDLC 2.4.2

**Status:** staged consumer candidate; do not merge until the immutable
`ai-sdlc-v2.4.2` control-plane release is published and verified.

This patch changes only the source consumer's reusable-router pin from the
published `ai-sdlc-v2.4.1` release to `ai-sdlc-v2.4.2`. It does not change
the closed v2 task payload, approval semantics, lifecycle ownership, or result
projection contract.

## Merge gate

Keep production on `ai-sdlc-v2.4.1` until all of these conditions are true:

1. the reviewed `consulting-playbook` target candidate is published as
   immutable `codex-adapter-v2.4.2` at its exact reviewed merge commit;
2. immutable `ai-sdlc-v2.4.2` is published at the exact reviewed control-plane
   commit;
3. the control-plane publication attestation records that tag identity and
   Runtime Preflight passes;
4. the organization registry resolves the enabled consulting target to the
   reviewed 2.4.2 adapter tag and evidence; and
5. the source-consumer PR CI passes on its final head.

Only then may this consumer repin merge. After merge, use a fresh governed task
identity for REAL verification; do not reuse terminal failed issue #145.

The published 2.4.1 consumer remains the rollback baseline and its immutable tag
must not be moved or reinterpreted.
